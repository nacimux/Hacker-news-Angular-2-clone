/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DomainPipe } from './domain.pipe';

describe('Pipe: Domain', () => {
  it('create an instance', () => {
    let pipe = new DomainPipe();
    expect(pipe).toBeTruthy();
  });
});
